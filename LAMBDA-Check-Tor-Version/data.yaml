Timeout: 10
Environment:
    TOPIC: "!ImportValue SNS-TOPIC"
    TORVERSION: "0.2.9.9"
Policies:
    - Effect: Allow
      Action:
          - sns:Publish
      Resource: "!ImportValue SNS-TOPIC"
Schedules:
    - "cron(0 18 * * ? *)"
