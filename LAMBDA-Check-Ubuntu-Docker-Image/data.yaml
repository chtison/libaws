Timeout: 10
Environment:
    TOPIC: "!ImportValue SNS-TOPIC"
Policies:
    - Effect: Allow
      Action:
          - sns:Publish
      Resource: "!ImportValue SNS-TOPIC"
Schedules:
    - "cron(0 18 * * ? *)"
